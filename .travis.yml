branches:
  only:
    - master
sudo: required
services:
  - docker
language: python
python:
  - "3.6"
notifications:
  email: false
script:
  - docker-compose -f docker-compose.test.yml up --build --exit-code-from test
